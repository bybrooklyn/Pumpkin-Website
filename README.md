# Pumpkin Site

Astro + TypeScript static site for pumpkinmc.org.

## Commands

```sh
bun install
bun run dev
bun run build
bun run preview
```

Static assets and legacy public CSS paths live in `public/`. Shared page chrome lives in `src/layouts/` and `src/components/`; browser behavior lives in `src/scripts/`.
